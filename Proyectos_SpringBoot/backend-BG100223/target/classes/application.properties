spring.application.name=backend-BG100223

# Configuraci�n del DataSource para la conexi�n a la base de datos MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/fakestore
spring.datasource.username=root
spring.datasource.password=

# Especificaci�n del nombre de la clase del driver JDBC de MySQL
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Propiedades de Hibernate
# Dialecto de Hibernate para MySQL (Actualmente ya no es necesario agregar esta l�nea)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Estrategia de creaci�n de tablas de Hibernate:
# - "update": Actualiza la estructura de la base de datos seg�n las entidades definidas en el c�digo.
# No elimina datos existentes.
spring.jpa.hibernate.ddl-auto=update

# Muestra las sentencias SQL generadas por Hibernate en la consola
spring.jpa.show-sql=true